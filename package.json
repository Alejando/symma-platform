{
  "name": "symma-platform",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "generate:openapi": "pnpm --filter api run generate:openapi",
    "generate:kotlin": "openapi-generator-cli generate -i apps/api/openapi.json -g kotlin -o apps/mobile/app/src/main/java --global-property models --additional-properties=packageName=com.symma.app.data.remote.model,serializationLibrary=gson,dateLibrary=string"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.29.0",
    "prettier": "^3.7.4",
    "turbo": "^2.7.5",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
